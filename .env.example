# Application URL for CORS and redirects
NEXT_PUBLIC_APP_URL=http://localhost:3000

# External Bypass API endpoint (optional, defaults to rblxbypass.com)
# BYPASS_API_URL=https://rblxbypass.com/api/bypass

# Data storage path (optional, defaults to .data directory)
# DATA_STORAGE_PATH=.data
